import DB from '../../src/lib/db';

require('../test.setup')();

describe('DB Class', () => {
  // describe('#getAllUsers', () => {
  //   it('gets all users', () => DB.getAllUsers()
  //     .then((results) => {
  //       expect(results).to.have.length.above(3);
  //     }));
  // });
});
